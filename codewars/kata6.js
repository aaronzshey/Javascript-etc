//https://www.codewars.com/kata/563b662a59afc2b5120000c6/train/javascript
const nbYear = (p0, percent, aug, p) => {
  // aug (inhabitants coming or leaving each year), p (population to surpass)
  let multiplier = asdf;
  let newPopulation1 = p0;
  let newPopulation2 = newPopulation1 + aug;
  let convertedPercent = percent / 100;
  //define population???
  let a = Math.log10(population / p0);
  let b = Math.log10(convertedPercent);
  let yr = a / b;

  do {} while (true);
};
